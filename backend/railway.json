{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "npx prisma db push && npm run build && npm run start",
    "restartPolicyType": "ON_FAILURE"
  }
}
